---
id: amf_setup
title: AMF project configuration
---

To run AMF in your application, you'll need add AMF to your dependencies and then initialize AMF. 


## Download AMF artifacts
You can download the necessary AMF artifacts from:
-   [Maven](https://repository-master.mulesoft.org/nexus/content/repositories/releases/com/github/amlorg/)
-   [NPM](https://www.npmjs.com/package/amf-client-js)


## Add AMF to your dependencies
The following example shows how to add AMF to your project using Gradle, Maven, or NPM.

//reviewer note: I'm confused by this section, as it's not clear to me how this is formatted or what it's showing. 

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs
    groupId="framework"
    defaultValue="gradle"
    values={[
        {label: 'Gradle', value: 'gradle'},
        {label: 'Maven', value: 'maven'},
        {label: 'NPM', value: 'npm'}
    ]}
>
<TabItem value="gradle">

```gradle
// add mulesoft repository
repositories {
    maven {
        url 'https://repository-master.mulesoft.org/nexus/content/repositories/releases'
    }
}
dependencies {
    compile 'com.github.amlorg:amf-client_2.12:x.y.z'
}
```

</TabItem>
<TabItem value="maven">

```
<dependency>
    <groupId>com.github.amlorg</groupId>
    <artifactId>amf-client_2.12</artifactId>
    <version>x.y.z</version>
</dependency>
```

</TabItem>
<TabItem value="npm">

NPM:
```bash
$ npm install --save amf-client-js
```

Yarn:
```bash
$ yarn add --save amf-client-js
```


</TabItem>
</Tabs>


NOTE: you may use the `-SNAPSHOT` versions of the artifacts at your own risk since those snapshot versions may contain breaking changes.

## AMF initialization code example

The following example shows how to initialize AMF in your application:

<Tabs
    groupId="languages"
    defaultValue="java"
    values={[
        {label: 'Java', value: 'java'},
        {label: 'JavaScript', value: 'js'}
    ]}
>

<TabItem value="java">

```java
import amf.client.AMF;

class App {
    public static void main(String[] args){
      AMF.init().thenApply(() -> {
              // ... your code
      });
    }
}
```

</TabItem>
<TabItem value="js">

```javascript
const amf = require('./lib/amf-client-module.js')

await AMF.init();

// ... your code
```

</TabItem>
</Tabs>

You can now start using AMF in your application. 
